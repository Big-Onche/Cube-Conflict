shader 0 "aTrousFilter" [   // was: lazyshader 0 "aTrousFilter"
    attribute vec4 vvertex;
    void main(void)
    {
        gl_Position = vvertex;
    }
] [
    uniform sampler2DRect tex0;
    uniform vec2 aTrousSize;
    uniform vec4 aTrousParams;
    fragdata(0) vec4 fragColor;

    float aTrousKernel(int kernelIndex)
    {
        if(kernelIndex <= 0) return 0.375;
        if(kernelIndex == 1) return 0.25;
        return 0.0625;
    }

    float aTrousGuide(vec4 sampleValue)
    {
        if(aTrousParams.z > 0.5)
            return max(sampleValue.r, max(sampleValue.g, sampleValue.b));
        return sampleValue.a;
    }

    void main(void)
    {
        vec2 pixelCoord = gl_FragCoord.xy;
        vec4 centerValue = texture2DRect(tex0, pixelCoord);
        vec4 accumValue = vec4(0.0);
        float totalWeight = 0.0;
        float dilation = max(aTrousParams.x, 1.0);
        float guideStrength = max(aTrousParams.y, 0.0);
        float centerGuide = aTrousGuide(centerValue);

        for(int offsetY = -2; offsetY <= 2; ++offsetY)
        {
            int absOffsetY = offsetY < 0 ? -offsetY : offsetY;
            float weightY = aTrousKernel(absOffsetY);
            for(int offsetX = -2; offsetX <= 2; ++offsetX)
            {
                int absOffsetX = offsetX < 0 ? -offsetX : offsetX;
                float weightX = aTrousKernel(absOffsetX);
                vec2 sampleCoord = clamp(
                    pixelCoord + vec2(float(offsetX), float(offsetY)) * dilation,
                    vec2(0.5), aTrousSize - vec2(0.5));
                vec4 sampleValue = texture2DRect(tex0, sampleCoord);
                float guideDiff = abs(aTrousGuide(sampleValue) - centerGuide);
                float guideWeight = exp(-guideDiff * guideDiff * guideStrength);
                float sampleWeight = weightX * weightY * guideWeight;
                accumValue += sampleValue * sampleWeight;
                totalWeight += sampleWeight;
            }
        }

        vec4 filteredValue = accumValue / max(totalWeight, 1.0e-5);
        fragColor = vec4(filteredValue.rgb, centerValue.a);
    }
]